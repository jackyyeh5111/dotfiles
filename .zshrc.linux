# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="robbyrussell"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in $ZSH/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment one of the following lines to change the auto-update behavior
# zstyle ':omz:update' mode disabled  # disable automatic updates
# zstyle ':omz:update' mode auto      # update automatically without asking
# zstyle ':omz:update' mode reminder  # just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
# zstyle ':omz:update' frequency 13

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# You can also set it to another string to have that shown instead of the default red dots.
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# Caution: this setting can cause issues with multiline prompts in zsh < 5.7.1 (see #5765)
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git) 

source $ZSH/oh-my-zsh.sh

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='nvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch $(uname -m)"

# Set personal aliases, overriding those provided by Oh My Zsh libs,
# plugins, and themes. Aliases can be placed here, though Oh My Zsh
# users are encouraged to define aliases within a top-level file in
# the $ZSH_CUSTOM folder, with .zsh extension. Examples:
# - $ZSH_CUSTOM/aliases.zsh
# - $ZSH_CUSTOM/macos.zsh
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
alias gs="git status"
alias ga="git add"
alias gce="git commit -e"
alias gca="git commit --amend"
alias gd="git diff"
alias gb="git branch"
alias gck="git checkout"
alias gh="git checkout HEAD"
alias dp="docker ps"
alias de="docker exec"
alias di="docker image"
alias dr="docker rm"

alias vimz="vim ~/.zshrc"
alias souz="source ~/.zshrc"
alias vimv='vim ~/.vimrc'
alias souv='source ~/.vimrc'

alias cdrun="cd /home/jackyyeh/memryx/MIX/runtime"
alias cdaccl='cd ~/memryx/MX_API/build/mx_accl'
alias cdmg='cd ~/memryx/MX_API/build/mxa_manager'
alias cdpy='cd ~/memryx/MX_API/mx_accl/pymodule/tests && workon mx2p2' 
alias cdpp='cd ~/memryx/MxPrepost && workon no_pip_memryx'
alias cdapi='cd ~/memryx/MX_API'
alias u='cd ..'
alias runmg='cd /home/jackyyeh/memryx/MX_API/build/mxa_manager && ./mxa_manager'
alias cdt='workon test && ~/memryx/Tahakom-Pipeline/Tahakom-video-pipeline/src'
alias cds='workon mx2p2 && ~/memryx/Tahakom-Pipeline/src'
alias cdsc='cd ~/memryx/MX_API/tools/dfp_schedule_bench'
alias cdfc='workon mx2p2 && ~/memryx/MX_API/samples/multi_dfp/cartoonizer_pose/src/cpp'
alias cdfp='workon mx2p2 && /home/jackyyeh/memryx/MX_API/samples/multi_dfp/conditional_pipe'
alias hr='herdr'

# Standard listing replacement
alias ls='eza --icons=auto'

# Detailed list view (replacing `ll` / `ls -l`)
alias ll='eza -l --git --header --icons=auto'

# Detailed list including hidden files (replacing `la` / `ls -la`)
alias la='eza -la --git --header --icons=auto'

# Quick tree view up to 2 levels deep
alias lt='eza -T -L 2 --icons=auto'

# Complete tree view including hidden files, ignoring .git
alias lta='eza -Ta -L 3 -I ".git" --icons=auto'

export PATH="$HOME/.local/bin:$PATH"
export VIRTUALENVWRAPPER_PYTHON=$(which python3)
export WORKON_HOME=$HOME/.virtualenvs
source $(which virtualenvwrapper.sh)
#workon mx
#source ~/mx/bin/activate

# # Set up fzf key bindings and fuzzy completion
# source <(fzf --zsh)


alias tk='tmux kill-server'
alias ta='tmux attach -t'
alias kill9='sudo kill -9 '
alias cdmix='cd /home/jackyyeh/memryx/MIX && source setup_env.sh'
alias cdpytest='workon no_pip_memryx && setupmix && cd ~/memryx/MIX/runtime/tests'
alias cdtest='cd /home/jackyyeh/memryx/MX_API/build/mx_accl'
alias initmgr='cd /run &&  sudo mkdir -p mxa_manager && cd mxa_manager && sudo touch 10000.sock 10001.sock 10002.sock && cd /run && sudo chmod -R 777 mxa_manager'
alias mm='workon mx2.0 && ~/memryx/elm_async_fps_fix_internal/multistream_cpp'
alias cdgi='/usr/include/gstreamer-1.0/gst'
alias cdd='cd ~/SideProjects/pybind11-collection/reproduce && workon pybind11'

export SPDLOG_LEVEL="debug"
export MX_API_HOME=/home/jackyyeh/memryx/MX_API

source /usr/share/doc/fzf/examples/key-bindings.zsh

# Fuzzy finder related
alias vimf='vim $(fzf --preview="batcat --color=always {}")'
alias cdf='cd $(fd --type d . | fzf)'

alias cpw='pwd | tee >(xclip -selection clipboard)'
alias cpaf='fzf --preview="batcat --color=always {}" | xargs realpath | tee >(xclip -selection clipboard)'
alias cpf='fzf --preview="batcat --color=always {}" | tee >(xclip -selection clipboard)'
ff() {
    "$@" | fzf | tee >(xclip -selection clipboard)
}

### Use fzf for preview git operation ###
# Navigate files with arrows
# TAB to multi-select
# Enter → diff
# CTRL-A → add
# CTRL-R → restore
git_interactive() {
  local out key files_array file

  # Run fzf with preview and key bindings
  out=$(
    git status -uno -s | awk '{print $2}' | \
      fzf -m \
        --bind "enter:accept" \
        --bind "ctrl-a:accept" \
        --bind "ctrl-r:accept" \
        --expect=enter,ctrl-a,ctrl-r \
        --preview 'git diff --color=always -- {}' \
        --preview-window=right:70%
  ) || return

  # First line = key pressed, rest = selected files
  key=$(head -n1 <<< "$out")
  files_array=()
  while IFS= read -r file; do
    [[ -n "$file" ]] && files_array+=("$file")
  done < <(tail -n +2 <<< "$out")

  if [[ ${#files_array[@]} -eq 0 ]]; then
    echo "No files selected."
    return
  fi

  # Perform action based on pressed key
  case "$key" in
    ctrl-a)
      git add "${files_array[@]}"
      echo "Added files:"
      for f in "${files_array[@]}"; do
        echo "$f"
      done
      ;;
    ctrl-r)
      git restore "${files_array[@]}"
      echo "Restored files:"
      for f in "${files_array[@]}"; do
        echo "$f"
      done
      ;;
    *)
      git diff "${files_array[@]}"
      ;;
  esac
}

alias gi='git_interactive'
alias bat='batcat'
alias grep='rg'
#alias find='fd'
alias vc='cd /home/jackyyeh/.config/nvim && nvim init.lua'  

# alias nvim='/usr/bin/nvim.appimage'
alias vim=nvim
alias vi='nvim'
alias v=nvim
export PATH="$PATH:$HOME/.local/share/nvim/mason/bin"

# nvm setting
#export NVM_DIR="$HOME/.nvm"
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
#[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

export PATH="$PATH:/home/jackyyeh/.nvm/versions/node/v24.11.0/bin"

export COLORTERM=truecolor

export PATH=$PATH:/usr/local/go/bin

# enable core dumps
# ulimit -c unlimited

. "$HOME/.cargo/env"

# format alias
alias mxstyle='astyle --align-pointer=type --align-reference=name --style=kr --add-braces --attach-return-type-decl --keep-one-line-statements --convert-tabs --close-templates --max-code-length=200 --pad-comma --pad-oper --indent-col1-comments --indent-preproc-define --indent-preproc-block --indent-labels --indent-switches --indent-modifiers --indent=spaces=4 --break-closing-braces --keep-one-line-blocks --lineend=linux'
export GST_PLUGIN_PATH=$HOME/memryx/MX_GStreamer/plugins

plugins=(git zsh-autosuggestions)

eval "$(starship init zsh)"
eval "$(zoxide init zsh)"

alias cl='claude code'
